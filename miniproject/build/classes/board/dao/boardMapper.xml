<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardSQL">
 	<!-- 게시판 글쓰기 -->
 	<insert id="boardWrite" parameterType="board">
		insert into board(seq,id,name,email,subject,content,ref) values(seq_board.nextval,#{id},#{name},#{email},#{subject},#{content},seq_board.currval)	
  	</insert>
  	<!-- 게시판 목록 -->
  	<select id="boardList" parameterType="java.util.Map" resultType="board">
  		select * from (select rownum rn, tt.* from (select seq,id,name,email,subject,content,ref,lev,step,pseq,reply,hit,to_char(logtime, 'YYYY.MM.DD') as logtime from board order by ref desc, step asc)tt) where rn&gt;=#{startNum} and rn &lt;= #{endNum}	
  	</select>
  	<!-- 전체 게시글 수 -->
  	<select id="getTotalA" resultType="int">
  		select count(*) from board
  	</select>
  	<!-- 게시글 보기 -->
  	<select id="getBoard" parameterType="int" resultType="board">
  		select * from board where seq=#{seq}
  	</select>
  	<!-- 조회수 증가 -->
  	<update id="boardHit" parameterType="int">
  		update board set hit = hit+1 where seq=#{seq}
  	</update>
  	<!-- 게시글 삭제 -->
  	<delete id="boardDelete" parameterType="int">
  		delete from board where seq=#{seq}
  	</delete>
  	<!-- 게시글 수정 -->
  	<update id="boardModify" parameterType="java.util.Map">
  		update board set subject=#{subject}, content=#{content}, logtime=sysdate where seq=#{seq}
  	</update>
</mapper>